[project]
name = "travel-tvb-shared"
version = "0.1.0"
description = "Shared Python helpers for Travel TVB microservices"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = true
